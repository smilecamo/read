
<view class="container">
  <image
    bind:tap="onLeft"
    class="icon"
    src="{{next?disLeftSrc:leftSrc}}" />
  <text class="text">{{text}}</text>
  <image
    bind:tap="onRight"
    class="icon"
    src="{{first?disRightSrc:rightSrc}}" />
</view>
